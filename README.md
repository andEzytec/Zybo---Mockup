
  # User Registration Flow

  This is a code bundle for User Registration Flow. The original project is available at https://www.figma.com/design/e68RodChfS6a3HgE3ETINm/User-Registration-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  